/** Minimal middleware — Clerk auth routes will be added in Sprint 1. */
export function middleware() {
  return;
}

export const config = {
  matcher: ['/((?!_next/static|_next/image|favicon.ico).*)'],
};
